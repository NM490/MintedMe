/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "nft-cdn.alchemy.com",
      "ipfs.io",
      "avatars.githubusercontent.com",
    ],
  },
};

export default nextConfig;
